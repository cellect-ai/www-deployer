# --- Infisical Configuration (Required) ---
INFISICAL_CLIENT_ID=            # Your Infisical universal-auth client ID
INFISICAL_CLIENT_SECRET=        # Your Infisical universal-auth client secret
INFISICAL_API_URL=              # Infisical API URL (e.g., https://app.infisical.com)
INFISICAL_WORKSPACE_ID=         # Your Infisical project ID
INFISICAL_ENV=                  # Environment (e.g., dev, prod)
INFISICAL_PATH=/                # Path in Infisical (default: /)

# --- Local Overrides (Optional - pulled from Infisical by default) ---
# These variables are pulled from Infisical. Only set locally if testing:
# WWW_GIT_DEPLOY_SECRET=        # Webhook secret (in Infisical)
# GITHUB_TOKEN=                 # GitHub PAT (in Infisical)
# WWW_GIT_REPOS_PATH=           # Repos path (in Infisical, default: /mnt/pve/nfs-cellect-repos)
# WWW_GIT_DEPLOY_PORT=          # External port (in Infisical, default: 9009)
